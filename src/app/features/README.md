# Features

Business capabilities belong here as independently routed vertical slices. Each feature owns its
pages, components, data access, state, models, and routes when those elements are feature-specific.

Features may consume core infrastructure and shared building blocks, but must not depend directly
on another feature's internal implementation.
