# Core

Application-wide infrastructure belongs here, including configuration, guards, interceptors,
singleton services, repositories, and global state.

The core layer must not import from feature modules.
